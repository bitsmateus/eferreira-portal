# Regra 8: nenhum segredo no repositório.
.env
.env.*
!.env.example

node_modules/
.next/
out/
build/
dist/

*.tsbuildinfo
next-env.d.ts

.DS_Store
Thumbs.db

coverage/
.vitest/

# dumps de backup do banco
backups/

.vscode/
.idea/

# permissoes locais do Claude Code — nao versionar
.claude/settings.local.json

# Modelos do escritorio: sao documentos REAIS preenchidos, com CPF, RG,
# nome da mae e endereco de clientes de verdade. Nao versionar (LGPD).
# O mapa de campos, sem dado pessoal, esta em docs/modelos-de-documento.md
docs/modelos/

# Scripts descartáveis de automação do painel de implantação. Ficam fora do
# repositório: são de uso único, apontam para o painel e não são a documentação
# da implantação — essa é docs/03-implantacao-easypanel.md.
ep-*.mjs
